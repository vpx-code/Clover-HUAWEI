package com.xvlaze.clover.watch.repository

import android.content.Context

class WatchRepository(c: Context) {
    //private val model = com.xvlaze.clover.watch.WatchConnector(c)
    //fun sendReminder() = model.sendReminder()
}